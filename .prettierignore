node_modules
.next
out
build
coverage
package-lock.json
*.log

# Prisma-generated client (regenerated by `prisma generate`).
src/generated

# Prisma-generated migration SQL — owned by the migrate CLI, never reformatted.
prisma/migrations

# The specification, its review artefacts and every prose document. Formatting
# churn here would bury the decision diffs that matter.
docs
ai-agents
*.md

# OUTSIDE LUCKY'S WRITE SCOPE (D-025, F-18). A PR touching a workflow requires
# explicit human authorship, and `prettier --write .` would otherwise rewrite
# these silently. Do not remove this entry to "tidy" the workflows.
.github

# Deployment descriptors — reformatting them produces a diff that reads like a
# deployment change and is not one.
docker-compose.yml
infra
environments
