{
  "name": "splashtrack",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "dev": "echo 'No application yet. See docs/build/phase-0-foundation.md 0.2 (extract the foundation from WebAppTemplate).' && exit 1"
  }
}
